# LIMU v20.59

三項都是實機回報，且都補上了對應的自動化斷言——同類問題不會再默默通過。

## 1）回首頁鈕壓住面板內容

浮動鈕是 `position:fixed`，我沒有替它在面板內保留空間，於是它直接蓋在
內容上：全域搜尋壓住「全部」篩選鈕與說明文字、輔導中心壓住「此時間範圍
內無紀錄」、今日工作台壓住「開啟今天的完整紀錄」。

面板底部留白改為 `calc(安全區 + 78px)`（按鈕 48 ＋ 下緣 18 ＋ 間隙 12）。

新增斷言：取按鈕中心點做 `elementFromPoint`，命中的必須是按鈕自己，
否則就是壓到別的元素。

## 2）圖示沒有對準圓心

原本用 `⌂`（U+2302）。這個字元在多數字體裡基線偏低、左右留白也不對稱，
靠 `align-items:center` 是救不回來的——實機看就是歪的。

改用 inline SVG 畫的房子，尺寸與位置完全由我們決定。

新增斷言：量 SVG 中心與按鈕中心的偏差，`dx`/`dy` 皆須 ≤ 1.5px。
目前實測 0.0 / 0.0。

## 3）功能選單的回首頁規格不一致

原本是清單裡的一列「⌂ 回到首頁」，其他三個面板卻是左下角的浮動圓鈕。
四個面板的操作位置不一致，肌肉記憶建立不起來。

已移除清單項目，改用與其他面板完全相同的 `.sheet-home-btn`。

新增斷言：功能選單開啟時，回首頁鈕必須是 48×48、含 SVG、位於左下角。

---

## 驗證

- `visual-check` **26/26**（新增 3 項）
- jsdom 端對端 32/32
- 無 IndexedDB 降級 7/7
- 115 個按鈕與月曆格巡覽零錯誤
- `bump-version` 同步 9 處版本識別後 `--check` 一致
- CSS 解析、各 script 區塊語法零錯誤
