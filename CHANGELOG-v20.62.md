# LIMU v20.62 — Venus Refinement 美術統一版

本版只收斂視覺語言、主題裝飾與主要導覽圖示；不變更 Venus OS、Firebase、
資料格式、總覽座號資訊、班級操作流程或 iPhone 安全區。

## 1. 共用視覺規格

- 建立共用卡片 18px、面板 28px、控制項 12px 與 22px／2px 線性圖示規格。
- 手機底部五個主要導覽改為同一套 inline SVG，不再依賴不同系統外觀不一的 emoji。
- 桌面頁首的今日、搜尋、輔導與功能選單同步使用相同圖示語言。
- 所有既有按鈕位置、44pt 以上觸控範圍與無障礙標籤均保留。

## 2. 慕夏・藍紫鳶尾

- 移除沿整個長頁拉伸、重複花朵的主框呈現。
- 主內容改為等比例縮放的固定角飾與細金線，卡片只保留低密度雙線收邊。
- 面板分隔帶改為 320×18 的專用向量線飾，不再把 1200×400 花飾壓進 34px 高度。
- 鳶尾具象圖只集中於標題、主題選擇與空狀態，降低重複造成的視覺疲勞。

## 3. 星雲

- 取消借用慕夏花卉外框，改為純星圖航線、星座節點與金紫角標。
- 空狀態改用專用星盤，不再顯示慕夏鳶尾花飾。
- 新增靜態星圖路線與可重複邊框切片；邊線使用 `round`，具象星圖維持原比例。
- 移除頁首無限發光動畫、彈窗模糊與選取按鈕的無限脈衝，保留靜態層次與選取辨識。
- 月份裝飾由花形 `❧` 改為星形 `✦`。

## 4. 奧德賽・遠征

- 新增低透明青銅羅盤、海圖航線與專用青銅框切片。
- 月曆框不再使用 `stretch`，角飾與線段分離處理。
- 頁首小字收斂為 `ΝΟΣΤΟΣ • ΙΘΑΚΗ`，降低英文字樣密度。
- 保留電影寫實人物只出現在開場、登入與 App Icon，不進入日常操作區。

## 5. 開場、登入與 Icon

- 登入背景改為沿用開場的電影寫實奧德賽影像，不再切換成另一套插畫畫風。
- 登入標誌改為共用線性剪貼簿圖示。
- App Icon 保留既有的日蝕、人像與船構圖；它已和開場同一世界觀，且小尺寸辨識核心清楚，
  本版不為了改版而重新生成臉部或髮型。

## 6. 新增主題素材

- `assets/mucha-divider-v20-62.svg`
- `assets/nebula-divider-v20-62.svg`
- `assets/nebula-empty-v20-62.svg`
- `assets/nebula-frame-v20-62.svg`
- `assets/nebula-route-v20-62.svg`
- `assets/odyssey-compass-v20-62.svg`
- `assets/odyssey-frame-v20-62.svg`

以上均為透明、無文字操作內容的輕量向量素材，並已加入離線預快取。

## 保留不動

- Venus OS 與 Venus Card
- Firebase 專案、Firestore 路徑、UID 分區與同步衝突邏輯
- IndexedDB／localStorage 與 `schemaVersion 7`
- 總覽上的個別座號資訊，以及 5／6／7 組動態欄位
- iPhone 頂部安全區、底部懸浮膠囊位置與內容底部預留
- v20.61 的臺灣日期、115 學年度行事曆與核心快取驗證

## 驗證

- 10 段內嵌程式碼與 Service Worker／工具程式語法通過。
- HTML 可解析，CSS 括號、字串與註解結構完整。
- 7 個新增 SVG 均通過 XML 解析，且全部列入 Service Worker 預快取。
- 部署原始碼中不再有裝飾用 `stretch` 或 `background-size:100% 100%`。
- 430×932、3× DPR 的真實 Chromium 回歸測試 37／37 通過，包含開場、懸浮工作欄、
  面板、班級設定、三套重製主題、橫向溢出與零 console 錯誤。
- Chromium 不能完整模擬 Safari 的安全區與首次觸控手感；部署後仍需以 iPhone 確認
  五套主題、首次滑動與 5／6／7 組版面。
