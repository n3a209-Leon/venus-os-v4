# LIMU v20.58

## 先報告一件事：v20.56 的修改其實沒有進到檔案

`tools/visual-check.mjs` 一寫好、第一次跑，就抓到「工作欄純圖示」沒生效。
回查原因：v20.56 那支批次腳本在中途拋出斷言錯誤，而寫檔在最後一行——
**該次的四項修改全部沒有寫入**。當時我只重跑了拋錯的那一段，
沒有回頭確認前面幾段是否已經落地。

所以你說「剛剛要更新的在新版本還是都沒有改掉」，是完全正確的。
以下三項是這一版才真正進去的：

- 工作欄改為純圖示（文字改視覺隱藏，保留給螢幕報讀器）
- 選取態不再撐開，改以填色圓形＋圖示放大 8%
- 面板底部留白移除多留的 58px（那正是深色主題下那塊黑面）

這也正是接下來兩支工具要解決的問題。

---

## 1）tools/bump-version.mjs — 版本號同步

版本識別散在 9 個地方（`index.html` 6 處、`sw.js` 3 處、`version.json` 3 欄），
手動同步已經出過兩次事：一次把 `sw.js` 清成 0 byte，一次差點讓開場的
版本號與 `APP_VERSION` 不同步——而那行版本號正是判斷快取有沒有換版的依據。

```bash
node tools/bump-version.mjs --check    # 只檢查
node tools/bump-version.mjs patch      # 進版並同步全部
```

寫檔前確認每個檔案非空、替換後確實含新版本識別；寫檔後再整份驗證。
任一步不過就中止，不留下改到一半的檔案。

## 2）tools/visual-check.mjs — 真實瀏覽器版面檢查

用 Chromium 以 iPhone 直式尺寸開啟 App，量實際的元素位置與 computed style。
**23 項斷言**，涵蓋這段時間所有反覆出問題的地方：

| 面向 | 斷言 |
|---|---|
| 開場 | 開場層與圖片是否滿版、掛在 body 底下、影像路徑無 transform、版本號 |
| 工作欄 | 懸浮位置、全圓角、無 backdrop-filter、純圖示 |
| 面板 | 圓角、把手、貼齊底緣、開窗時工作欄收起 |
| 畫布 | **底色是否真的同步為面板底色**、關窗後是否還原 |
| 回首頁鈕 | 左下角、48×48 圓鈕、圖示為 `⌂` |
| 班級設定 | 無巢狀捲動容器、未使用 `content-visibility:auto` |

其中「畫布底色是否真的同步」就是連續兩版被當成已修復、
實際上從未被驗證過的那一條。

腳本自帶臨時靜態伺服器（`file://` 會因 react-dom 帶 `crossorigin`
被 CORS 擋掉），並自動尋找可用的 Chromium。`--shots out/` 可存四張截圖。

### 誠實說明它的極限

Chromium 不是 Safari。iOS 特有的行為——畫布高度時而 873pt 時而 932pt、
Service Worker 換版時機、`signInWithRedirect`——這裡重現不了。
真機測試仍然不能省。這支負責的是「版面與樣式有沒有照我們寫的生效」，
而這正好是過去十幾個版本裡最常出錯、也最沒人把關的一環。

## 3）文件與清理

- 新增 `README-工具說明.md`，含建議的改版流程
- CHANGELOG 只保留最近四版

---

## 驗證

- `visual-check` 23/23 通過（含四張截圖目視確認）
- jsdom 端對端 32/32
- 無 IndexedDB 降級 7/7
- 116 個按鈕與月曆格巡覽零錯誤
- `bump-version --check` 一致
- CSS 解析、各 script 區塊語法皆零錯誤
