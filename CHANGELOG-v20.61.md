# LIMU v20.61 — 穩定版

本版只處理日期、行事曆、版本快取與 iPhone 點擊負擔；不新增大型功能，
不變更 Venus OS、Firebase 路徑、資料格式、總覽座號資訊或主題版面。

## 1. 臺灣本地日期修正

- 輔導紀錄日期改用本地日期初始化。
- 日期前一天／後一天改用純日期位移，不再以本地午夜轉成 UTC 字串。
- 組別版本的「目前生效」判斷全部改用既有 `today()` 本地日期。
- 備份檔名日期改用本地日期。
- 保留 `toISOString()` 作為同步時間戳；它們不是畫面日期，不應改成本地字串。

## 2. 114／115 學年度內建共同日期

- 新增 115 學年度共同日期：第一學期 2026-08-31 開學、2027-01-20
  休業；第二學期 2027-02-11 開學、2027-06-30 休業，以及共同假日。
- 保留 114 學年度共同日期供歷史班級還原。
- 內建資料不再放入單一學校的評量、運動會或畢業典禮日期；這些仍由公開
  行事曆匯入，避免套錯學校。
- 新增班級若沒有自己的行事曆，會依該班學年度載入內建日期，不再繼承切換前
  班級的全域舊行事曆。
- 115 學年度轉換助手預設為 2026-08-31／2027-02-11；JSON 留白時會載入
  對應學年度的內建共同日期。
- 「載入本學年度內建日期」改為先確認再覆蓋，並正常寫回班級專屬資料。

既有已儲存或已匯入的行事曆不會自動被覆蓋。

## 3. 移除全域 Ripple

刪除每次點擊都會量測按鈕、插入節點並暫時改寫 `position`／`overflow` 的全域
波紋。按鈕功能、觸控範圍與原有選取狀態動畫均保留。

## 4. 版本與離線快取

- `bump-version` 升版時會以 `Asia/Taipei` 產生新的建置日期，不再沿用前一版。
- 工具現在同步並驗證 `APP_BUILD_DATE` 與 `version.json.buildDate`。
- Service Worker 安裝時先交叉驗證 `index.html`、`sw.js`、`version.json`，再將
  三個核心檔一起寫入同一版快取。
- 版本：`20.61.0`
- 建置：`limu-teacher-v20-61-20260804`
- 快取：`hw-tracker-v20-61`
- 資料格式：`7`（未變更）

## 保留不動

- Venus OS 與獎卡系統
- Firebase 專案、Firestore 路徑、UID 分區與同步衝突邏輯
- IndexedDB／localStorage 資料格式與 `schemaVersion 7`
- 總覽上的個別座號資訊
- iPhone 頂部／底部安全區、底部 44pt 以上觸控範圍
- 班級設定首次滑動、面板 sticky 回首頁與現有五套主題

## 驗證

- 10 段內嵌程式碼語法通過。
- `sw.js` 與兩支工具程式語法通過。
- 版本、建置、快取與建置日期一致性通過。
- 23 個預快取素材全部存在，三個核心檔快取規則通過。
- 日期位移分別在 `Asia/Taipei` 與跨日的美西時區測試通過。
- 114／115 學年度內建資料、115 學年度預設開學日與防止共用物件污染測試通過。

本次執行環境沒有 Chromium 執行檔，因此 27 項視覺瀏覽器檢查未冒充為已通過；
部署前仍需以 iPhone 真機確認首次滑動、日期前後鍵與安全更新一次。
