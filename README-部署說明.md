# LIMU 教師專用小工具 v20.22

把本資料夾內的檔案與 `assets` 資料夾完整上傳到同一個網站根目錄。請勿只更新 `index.html` 或 `sw.js`，三個版本識別檔必須同時上傳：

- `index.html`
- `sw.js`
- `version.json`

本版版本識別為：

- App：`20.22.0`
- 建置：`limu-teacher-v20-22-20260731`
- 快取：`hw-tracker-v20-22`
- 資料格式：`7`（與 v20.15 相同，不觸碰既有教師資料）

v20.16～v20.22 皆為修補版，不新增功能。重點修正一個會讓 App 完全卡死的登入死結、
以及 Android 圖示被裁切、iPhone 主畫面名稱錯誤、離線素材缺漏等問題。
完整清單見 `CHANGELOG-v20.22.md`。

## 這一版務必整包上傳

圖示檔名已由 `v20-15` 改為 `v20-16`，且**新增**一個 maskable 圖示。
請確認以下檔案都有上傳，缺任何一個都會影響安裝後的圖示顯示：

- `app-icon-192-v20-16.png`
- `app-icon-512-v20-16.png`
- `app-icon-512-maskable-v20-16.png` ← 新增
- `assets/apple-touch-icon-v20-16.png`
- `manifest.webmanifest`

舊的 `*-v20-15.png` 可以刪除。

Service Worker 的素材預快取已改為容錯：單一檔案上傳失敗不會再讓整個 SW 裝不起來，
但缺漏的檔名會列在瀏覽器 Console，部署後值得看一眼確認沒有紅字。

## 升級注意

從 v20.13 或更早版本升級時，舊本機資料會在登入後搬到該 Google UID 的專屬分區。
搬移完成前不會刪除來源；驗證完成後才清掉未分區副本。

v20.15 若在此階段發生錯誤會導致畫面永久卡在開場動畫（全黑無反應），本版已修正：
即使資料分區失敗，也會顯示錯誤訊息並讓 App 回到可操作狀態。

iPhone 主畫面若仍保留舊圖示或舊名稱「作業記錄」，移除舊捷徑後重新
「加入主畫面」即可。

網站行事曆若受校網 CORS 限制，請下載公開 `.ics`、`.csv` 或 `.json` 檔，再用 App 內的「選擇已下載的檔案」匯入。這是本版不依賴付費伺服器的正式備援流程。
