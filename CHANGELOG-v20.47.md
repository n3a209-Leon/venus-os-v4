# LIMU v20.47

## 開場黑塊：排版早就對了，是繪製沒跟上

v20.46 量到的數字：

```
scr932 inn932 cli873 vis932
dvh932 svh873 lvh932 ovl932
wrp932 img1011 sat59 sab34 dpr3 SA
```

- `ovl932`：開場層 932 ✓
- `wrp932`：圖層 932 ✓
- `img1011`：932 × 1.085（開場動畫的 scale）✓

**排版上每一層都已經滿版了，但畫面上圖仍然切在 873pt。**
排版對、繪製不對。

### 對照組排除了兩個假設

翻回 App 主畫面的截圖（IMG_4819）逐列取樣：深色底 `rgb(13,13,37)`
一路畫到 y=930.7pt 沒有任何斷點，底部工具列（`position:fixed; bottom:0`）
也確實坐在 932。所以：

- 不是 `position:fixed` 被裁到 873
- 不是視窗真的只有 873

只有開場那一層有問題。

### 原因

開場圖層在啟動極早期建立，當時視窗還是 873（v20.45 那次量測就抓到
`inn873 dvh873`），裁切範圍因此定在 873。之後視窗長到 932，但這一層掛著
`will-change:transform` 的動畫、在合成器上獨立跑，**不會重新光柵化**，
舊的裁切範圍就一直留著。這也解釋了為什麼時好時壞——
看那次啟動有沒有踩到視窗還沒定案的空窗期。

### 修正

監聽 `resize` / `orientationchange` / `visualViewport.resize`，
另外在開場的前 1.5 秒每 250ms 比對一次 `innerHeight`
（事件有可能在 React 掛載前就發生完了）。高度一有變化就遞增計數器，
並把計數器當作圖層的 `key`——React 會重建該節點，
強迫產生一個裁切範圍正確的新圖層。

量測列多印 `tick`，顯示這次啟動重建了幾次。
`tick0` 代表沒踩到空窗期，`tick1` 以上代表補救機制有作動。

## 修正路徑回顧

| 版本 | 假設 | 結果 |
|---|---|---|
| v20.41 | 包含區塊被縮短 → `dvh` | ✗ 開場層本來就沒問題 |
| v20.43 | 祖先裁切 → `portal` | ✗ 同上 |
| v20.44 | （加量測）| ✓ 排除「開場層短了」 |
| v20.45 | 百分比推算錯 → 圖層也用 `dvh` | ✗ 但抓到 dvh 會跳動 |
| v20.46 | `dvh` 不穩 → 改 `lvh` | ✗ 但確認排版已滿版 |
| v20.47 | 合成圖層裁切範圍過期 | 待實機驗證 |

前三版是連續猜測，第四版才開始量。若 v20.41 沒中就直接加量測，
中間至少省下兩個版本。這個教訓已經記下來了。

---

## 驗證

- 正常環境端對端 32/32
- 無 IndexedDB 降級 7/7
- 145 個按鈕與月曆格自動巡覽，零錯誤
- 開場層 portal 掛載點正確、退場乾淨移除
- JS 各區塊語法、CSS 解析零錯誤

## 部署後

看開場左上角：黑塊應消失。`tick` 的數字告訴我們這次有沒有踩到空窗期。
確認後下一版移除整段量測列。
