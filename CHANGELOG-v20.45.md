# LIMU v20.45

## 開場黑塊：實機數字讓原因定案

v20.44 在開場畫面印出的量測結果：

```
scr932  inn932  cli873  vis932
dvh932  svh873  lvh932  ovl932
sat59   sab34   dpr3    SA
```

關鍵是最後一項 `ovl932`：**開場層本身已經是滿版的 932pt**。
所以底下那塊 `#07040f` 不是 html 的底色，是開場層自己的背景——
真正停在 873pt 的是**裡面那張圖**。前三版全都在修錯的東西：

| 版本 | 改了什麼 | 為什麼沒用 |
|---|---|---|
| v20.41 | `inset:0` → `height:100dvh` | 開場層本來就沒問題 |
| v20.43 | `createPortal` 掛到 body | 同上，祖先從來不是原因 |
| v20.44 | （只加量測） | — |

另一個關鍵是 `cli873`：`documentElement.clientHeight` 只有 873，
正好是 `svh`（小視窗高度），也正好是 932 − 59（頂部安全區）。

`.spl-img-wrap` 用 `position:absolute; inset:0`，圖片用 `height:100%`——
兩者都是靠推算取得高度。在這台裝置上 WebKit 是照 873 算的，
沒有跟著父層實際的 932。底部就空出 59pt，露出開場層的背景色。

### 修正

圖層與圖片都改成直接指定絕對長度，完全不經過百分比／偏移量推算：

```css
.spl-img-wrap {
  position:absolute;
  top:0; left:0; right:0;
  height:100vh;      /* 舊瀏覽器 */
  height:100dvh;     /* 實機量到 dvh = 932，是對的 */
}
.spl-img-wrap img {
  width:100%;
  height:100vh;
  height:100dvh;
}
```

`dvh` 在這台裝置上實測是 932（`dvh932`），是可信的來源；
出問題的是 `100%` 與 `inset` 這兩條推算路徑。

量測列同時補上 `wrp`（圖層高度）與 `img`（圖片高度）。
若這次仍未修好，這兩個數字會直接指出是哪一層還在 873。

## 一併保留

- v20.44：班級設定頁的巢狀捲動容器（`.settings-view` 的
  行內 `overflowY:auto`）已在手機端關閉，只留頁面一個捲動層。
- v20.43 的 portal、v20.41 的 dvh 與底色都保留。它們不是這個問題的解，
  但都是更穩健的寫法，沒有理由退回去。

---

## 驗證

- 正常環境端對端 32/32
- 無 IndexedDB 降級 7/7
- JS 各區塊語法、CSS 解析零錯誤

## 部署後

看開場左上角：黑塊應該消失，`wrp` 與 `img` 兩個數字應該都是 932。
若確認修好，下一版就把量測列拿掉。
