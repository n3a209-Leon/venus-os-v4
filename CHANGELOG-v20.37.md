# LIMU v20.37（班級分頁起滑修正版）

## 修正

- 修正「更多 → 班級設定 → 班級」分頁按鈕成為滑動起點時，iPhone Safari 先判定點擊、導致頁面起步像被拉住的問題。
- 設定分頁列與三顆分頁按鈕明確使用 `touch-action: pan-y`，垂直拖曳直接交給原生捲動。
- 手機上目前已選取的分頁略過觸控命中；重複點擊本來就沒有動作，因此不影響功能。
- 設定分頁按住時不再套用共用的 `filter` 效果，避免 Safari 在起滑瞬間建立額外合成圖層。

## 保留

- 保留 v20.36 的 iPhone 頂部安全區與手機靜態主題層。
- 未更動班級資料、座號、Firebase 路徑、資料格式、其他按鈕或 Venus OS。
- 資料格式維持 schema v7。
