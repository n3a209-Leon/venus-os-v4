# LIMU v20.67 — 最終版

版本：20.67.0
建置：limu-teacher-v20-67-20260804
快取：hw-tracker-v20-67
資料格式：7（未變動）

累積包含 v20.65、v20.66，部署這一包即可。

---

## 修正：星雲主題把 `100dvh` 後備蓋掉了

`.app` 的高度原本寫成兩段式：

```css
.app{ min-height:100vh; min-height:100dvh; ... }
```

先寫舊單位當後備、再用新單位覆蓋，這是正確寫法。但星雲主題的根規則是：

```css
[data-theme="nebula"]{ ... min-height:100vh; }
```

`[data-theme="nebula"]` 與 `.app` 特異度相同（0,1,0），而且出現在檔案後面，
**等於把 dvh 那一行整個蓋掉**——只有星雲主題退回純 `100vh`。

`100vh` 不含 iOS Safari 的網址列，版面因此比可視區高，底部多出一段捲動。
獨立 App 模式沒有動態網址列，看不出差別，所以一路沒被抓到；用瀏覽器開才會出現。

已照原樣補回兩段式寫法。全檔複掃確認再無同類問題（keyframes 內的
`translateY(100vh)` 不算，那是動畫位移不是版面高度）。

## 修正：17 個純符號按鈕沒有可及名稱

只有 `×`、`‹`、`◄`、`►`、`+` 當內容、又沒有 `aria-label` 的按鈕，
螢幕閱讀器只會念出那個符號本身。清查後補齊 17 處：

| 位置 | 補上的名稱 |
|---|---|
| 週作業計畫 關閉 × 5 | 關閉週作業計畫 |
| 週計畫返回 ‹ × 4 | 返回週計畫列表 |
| 週切換 ◄ ► | 上一週／下一週 |
| 週計畫新增 + × 2 | 新增這一天的作業 |
| 週計畫移除 × × 2 | 移除這一項作業 |
| 作業列刪除 × | 刪除這一列作業 |
| 作業選擇器 關閉 × / 返回 ‹ | 關閉作業選擇器／返回上一步 |
| 輔導紀錄中心 關閉 × | 關閉輔導紀錄中心 |

`aria-label` 總數由 58 增為 77。**畫面完全沒有變動**——只加屬性，
沒有動任何 class、style 或結構。

---

## 刻意保留、未修改的兩項

這兩項稽核有查到，判斷「改動的風險大於收益」，記錄在此而不動它。

### 觸控目標小於 44pt

| 元件 | 目前尺寸 | 位置 |
|---|---|---|
| `.nav-btn` | 28×28 | 頁首月份前後切換 |
| `.undo-btn` | 30×30 | 頁首復原 |
| `.wp-add-btn` | 高 32（寬滿版） | 週計畫新增 |
| `.gr-del-btn` | 36×36 | 輔導紀錄刪除 |

**不改的理由**：標準做法是用 `::after` 撐開觸控範圍而不動版面，但
`.undo-btns` 的 flex `gap` 只有 3px，撐開後相鄰按鈕的觸控範圍會互相重疊，
反而可能造成反方向誤觸；直接放大尺寸則會動到已經調過五個版本的頁首排版。
在沒有實際誤觸回報之前，這個交換不划算。

若日後真的常誤觸，正確做法是**先把 `.undo-btns` 的 gap 拉到 8px 以上**，
再撐開觸控範圍，而不是只做後者。

### `.ov` 的 `backdrop-filter: blur(3px)`

星雲主題已在 v20.36 移除毛玻璃（該處註解記錄了原因），但基礎主題的
彈窗遮罩 `.ov` 仍保留 3px 模糊。它是單一 `position:fixed` 元素、不是重複元素，
且遮罩後方的頁面在彈窗開啟期間不變動，快照可以重用，成本比當初移除的
那批低得多。

**判斷方式**：若 `?perf=1` 顯示「格子重建」已是 0、但開關彈窗仍明顯停頓，
這裡就是下一個要動的地方。在拿到那個讀數之前不該先改。

---

## 三版累積驗收清單

| 版本 | 項目 |
|---|---|
| v20.65 | 月曆格子不再整片重建；`?perf=1` 量測探針 |
| v20.66 | 學年度轉換不再用過期學校 profile；當日工具不再靜默失敗；移除 114KB 孤兒素材；`noopener`；「更多」按鈕 ARIA；面板固定 key；圖示名稱守衛 |
| v20.67 | 星雲主題 dvh 後備；17 個符號按鈕的可及名稱 |

### 驗證紀錄

- 11 段內嵌程式 + 合併後全域範圍語法：通過
- `sw.js`、2 支工具程式語法：通過
- ESLint 錯誤規則：30 項，與 v20.64 起完全相同（皆為 `window.*` 全域與 UMD 誤判），三個版本下來未新增任何一項
- `react-hooks/rules-of-hooks`：零違反
- 迴歸斷言 12 項全數通過（含 v20.65／v20.66 全部修正仍在位）
- 版本識別一致性（index.html 7 處、sw.js 3 處、version.json、README）：通過
- 預快取 28 項對照磁碟：無缺檔
- HTML 標籤配對：平衡；重複 DOM id：無
- `JSON.parse` 47 處全部在 `try` 內或走 `safeJSON`
- `console.log` 殘留：0；`debugger`：0

**未做真實瀏覽器驗證**——Chromium 在本次作業環境無法下載。
`tools/visual-check.mjs` 與 iPhone 實測仍不能省。

### 建議的實測順序

1. `?perf=1` 停在月曆，做幾個不換月份的操作，看「格子重建」是否為 0。
   這是三版裡唯一還沒拿到實測結論的項目。
2. 星雲主題用 **Safari 瀏覽器**（不是主畫面 App）開一次，確認底部沒有多餘捲動。
3. 週作業計畫走完一輪：新增 → 編輯 → 切換週 → 移除項目 → 關閉。
4. 當日紀錄 → 更多 → 技術診斷／獎卡資料掃描。
5. 五套主題各看一次功能面板與底部列，確認圖示沒有變成三個點。
