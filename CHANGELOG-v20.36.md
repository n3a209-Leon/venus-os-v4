# LIMU v20.36（iPhone 安全區與原生捲動修正版）

本版以 v20.35 為基礎，只修正 iPhone 頂部重疊與滑動黏滯；不新增功能，不變更資料格式、班級資料、座號、Firebase 路徑或 Venus OS。

## 頂部安全區

- 移除 `body` 行內的 `padding:0`。該行內樣式會蓋掉 `env(safe-area-inset-top)`，使 PWA 工作列侵入動態島、時間、訊號與電量區域。
- 頂部安全區改由頁面實際佔位，工作列維持原高度與內容配置，不使用額外巨大空白。
- 不恢復 v20.34 曾使用的全域 `* { touch-action: manipulation !important; }`。

## 原生捲動

- iPhone 回到 Safari 預設 `touch-action:auto`，不再要求每一個表格格子與按鈕參與整頁手勢判定。
- 滑動排程器移除逐幀 `touchmove` 回呼；以 `touchstart/end` 判斷手指狀態、以 `window scroll` 延續慣性捲動保護。
- 手機版班級資訊列與設定分頁列不再使用 `position:sticky`，避免與全螢幕主題層反覆重組合成圖層。
- 星雲的兩個全螢幕 fixed 偽元素合併成單一隨頁面移動的靜態星空背景；保留藍紫星雲與星點視覺。
- 手機版移除主題長頁裝飾的濾鏡、混合模式與無限光暈動畫；圖片邊框、主題色與按鍵狀態仍保留。
- 月曆、摘要、出勤、抵免、回條與封存區塊改回一般排版，不再使用大面積 paint containment。

## 保持不變

- 資料 schema 維持 7。
- 不含 `?perf`、效能探針、診斷按鈕或正式部署 QA 程式。
- 離頁／切到背景時的資料 flush、滑動期間延後同步與合併儲存仍保留。
