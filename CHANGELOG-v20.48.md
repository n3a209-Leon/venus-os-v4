# LIMU v20.48

## 開場黑塊：那塊區域網頁畫不到

v20.47 的量測讓這件事沒有模糊空間：

```
scr932  inn873  cli873  vis873
dvh873  svh873  lvh932  ovl932
wrp932  img1009
late inn873 cli873 wrp932 tick2
```

- `inn` / `cli` / `vis` / `dvh` 四項全部是 **873**
- 1.2 秒後重量（`late`）仍是 873——不是啟動瞬間的過渡，整場都是 873
- `lvh932` 只是「工具列全收起時的理論最大值」，不代表實際可視區
- `wrp932` 代表圖層確實有 932，但**超出 873 的部分在畫面外**
- `tick2` 代表重建機制有作動，但重建再多次也沒用

結論：**這次啟動，網頁的可視區域就是 873pt。螢幕下方那 59pt
不屬於網頁，是 iOS 用頁面底色自己填的。** 前面幾版都在想辦法把圖畫到
那裡，而那塊區域在畫布上根本不存在。

另外 `inn` 在不同次啟動之間會在 932 與 873 之間跳（v20.46 那次是 932），
所以同樣的程式碼有時看起來正常、有時不正常——這也是判斷一直被誤導的原因。

### 修正

既然畫不到，就讓它看不出來：開場期間把 `<html>` 的底色換成圖片底緣的顏色。

```css
html.limu-splash-bg, html.limu-splash-bg body { background:#202024!important; }
```

`#202024` 取自實機截圖切線上方最後 4pt 的全寬平均值。
class 在開場元件掛載時加上、卸載時移除，不影響 App 畫面
（App 本身是深色底，那條在 App 裡本來就看不出來，所以你從來沒回報過）。

行內 `style="background:#07040f"` 的優先權由樣式表的 `!important` 蓋過。

## 修正路徑回顧

| 版本 | 假設 | 結果 |
|---|---|---|
| v20.41 | 包含區塊被縮短 → `dvh` | ✗ |
| v20.43 | 祖先裁切 → `portal` | ✗ |
| v20.44 | 加量測 | ✓ 排除「開場層短了」 |
| v20.45 | 百分比推算錯 → 圖層改 `dvh` | ✗ 但抓到 dvh 會跳動 |
| v20.46 | dvh 不穩 → 改 `lvh` | ✗ 但確認排版已滿版 |
| v20.47 | 合成圖層裁切過期 → 重建圖層 | ✗ 但拿到決定性數字 |
| v20.48 | 那塊區域網頁畫不到 → 染成同色 | 待驗證 |

七個版本。前三版是連續猜測，沒有先量；量了之後又因為數值會跳動而繼續誤判。
「推測性修正只允許一次，第一次沒中就直接植入量測」這條原則，
就是從這一題來的。

---

## 驗證

- 開場 class 生命週期：掛載時加上、卸載後 `<html>` class 清空
- 正常環境端對端 32/32
- 無 IndexedDB 降級 7/7
- JS 各區塊語法、CSS 解析零錯誤

## 若這次仍看得出來

代表 `#202024` 與你眼睛看到的底緣色仍有落差，回報一次即可微調——
這是純數值調整，不需要再改結構。
量測列會保留到你確認為止，確認後移除。
