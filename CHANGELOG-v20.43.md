# LIMU v20.43

## 先說結論：前一版可能根本沒跑到手機上

比對兩張實機截圖（IMG_4812 / IMG_4813），開場圖的下緣切在
**完全相同的一條線上：y = 2618px（872.7pt）**，一個像素都沒差。
而該列的顏色是 `(7,11,15)`，對應舊版 `.spl-overlay` 的背景 `#080b10`；
v20.41 已經把它改成 `#07040f`。

也就是說：手機上跑的很可能還是舊的建置。PWA 的 Service Worker 會沿用
既有快取，上傳新檔之後不一定會立刻換版。

**本版起，開場畫面左上角會顯示版本號**（例如 `v20.43.0`）。
以後回報畫面問題時，那行字就能直接證明手機上跑的是哪一版，
不用再猜。這是前兩次來回都卡住的原因。

## A. 開場畫面改用 portal 掛到 body

即使版本確實更新了，`height:100dvh` 也不見得夠。開場層原本是 App 的
early return，會落在 `#root` 底下。只要任何一個祖先具備
`transform` / `filter` / `contain` 之類的屬性，`position:fixed` 的包含區塊
就會變成那個祖先；若它同時有 `overflow:hidden`，**連 `100dvh` 都會被裁掉**，
量到的結果就會是 932−59＝873pt——正好符合實機數據。

改用 `ReactDOM.createPortal(..., document.body)`，讓開場層成為 body 的
直接子節點，任何祖先都影響不到它。這是不依賴任何假設的作法。

v20.41 的 `height:100dvh` 與 `background:#07040f` 一併保留：
三層防護（portal 不被裁、dvh 不看祖先、底色與 html 一致）任一層生效即可。

## B. 前幾版的修正一併帶上

這一版包含尚未在實機驗證過的：

- **v20.42**：從功能選單進入班級設定時，捲動鎖把「上一頁」的捲動位置
  還原到新頁上，導致一進來就停在中段，加上版面還在長，
  手指一滑就被錨定拉回去——也就是「被拉住、黏住」。
  已改為切換主檢視時捲動歸零。
- **v20.41**：中文字體改 `display=optional`，消除字體換上時的整頁位移。
- **v20.40**：IndexedDB 不可用時不再永久擋住登入。
- **v20.39**：`.app` 高度不再多扣一個安全區。

---

## 驗證

- 開場層 portal：確認掛載點是 `BODY` 的直接子節點，版本標記正確顯示，
  退場後乾淨移除
- 正常環境端對端 32/32
- 無 IndexedDB 降級 7/7
- 145 個按鈕與月曆格自動巡覽，零錯誤
- 強制關閉資料存活
- 各 script 區塊語法、CSS 解析皆零錯誤

## 部署後請這樣確認

1. 上傳整包（`index.html`、`sw.js`、`version.json` 三個都要）。
2. 把 App 從背景完全滑掉，重新開啟。
3. **看開場左上角的版本號**。如果不是 `v20.43.0`，就是快取還沒換版：
   再完全關閉、重開一次；仍舊沒換的話，把 App 從主畫面移除後
   重新「加入主畫面」。
4. 版本號確定是 v20.43.0 之後，再看黑框與黏住還在不在。
