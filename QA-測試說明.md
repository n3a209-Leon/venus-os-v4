# LIMU v20.22 測試說明

## 自動檢查

```bash
node qa/run-all-tests.js
```

分為三支：

| 檔案 | 內容 |
|---|---|
| `run-static-tests.js` | 版本一致、JavaScript 語法、資源存在、功能字串存在 |
| `run-logic-tests.js` | UID 資料分區、舊資料遷移、離線佇列、加密備份等邏輯 |
| `run-regression-tests.js` | **v20.16 起新增**：針對 v20.15 實際出貨缺陷的迴歸測試 |

### 關於迴歸測試

`run-static-tests.js` 的檢查絕大多數是 `html.includes('某字串')`，
驗證的是「這段字有沒有被寫進去」，不是「這段邏輯對不對」。
v20.15 的 21 項缺陷它全數綠燈通過。

`run-regression-tests.js` 補上會真正失敗的檢查，包含：

- `onAuthStateChanged` 的所有 return 路徑都必須解鎖 `authLoading`（P0 死結）
- `controllerchange` 必須區分首次接管與真的換版
- 原始碼不得出現雙重跳脫的 `\n`（白名單放行 ICS 反跳脫）
- **maskable 圖示的外圈 9% 必須是純底色**（內含極簡 PNG 解碼器，無外部相依）
- `apple-mobile-web-app-title` 必須等於 manifest 的 `short_name`
- `<meta theme-color>` 必須等於 manifest 的 `theme_color`
- SW 預快取必須涵蓋頁面引用的全部 assets 與 manifest 圖示
- 預快取清單中的檔案必須真的存在
- 素材預快取必須採容錯、靜態資源比對必須 `ignoreSearch`
- 開場圖必須 preload 且位置早於 ReactDOM
- 退場動畫的 JS 等待時間不得短於 CSS 動畫時長
- `_errLog` 必須有上限、寫入必須 debounce
- 主題監聽器必須使用 `attributeFilter`
- `reg.update()` 必須節流
- 備份解密必須夾住 iterations 上限

新增功能時若動到上述任一機制，這支測試會直接紅燈。

## 部署前真機回歸

1. 舊版已有資料的 iPhone 首次開啟，登入原 Google 帳號，確認班級、每日資料、週計畫與輔導紀錄均保留。
2. 新增一筆離線紀錄後恢復網路，確認同步狀態轉為完成。
3. 正在輸入輔導／週計畫／行事曆時發現更新，按「安全更新」，重新載入後確認草稿恢復。
4. 切換另一個 Google 帳號，確認不會看到原帳號本機資料；切回原帳號後資料仍在。
5. 匯出加密備份、顯示／隱藏密碼、測試錯誤密碼及正確密碼還原。
6. 切換五套主題，確認慕夏與奧德賽素材載入，所有按鈕仍可點擊。
7. 從 iPhone 主畫面冷啟動，確認直幅女神完整鋪滿螢幕、日蝕光暈與海面微光正常，輕觸可略過且不會卡住登入。
8. 重新加入主畫面，確認圖示為新版日蝕女神，**名稱顯示為「LIMU」而非「作業記錄」**，沒有白邊、舊月曆圖或異常裁切。

### v20.16 起新增的項目

9. **離線素材**：首次安裝並確認 SW 完成後，開飛航模式，依序切換五套主題，
   確認慕夏邊框、奧德賽海圖、星雲邊緣全部正常顯示，沒有破圖。
10. **首次安裝不得誤報更新**：以全新瀏覽器設定檔（或清除網站資料後）開啟，
    確認畫面底部**不會**出現「新版本已就緒」的藍色橫幅。
11. **登入失敗可回復**：開啟 Safari 無痕模式或以另一分頁佔住 IndexedDB 後登入，
    確認會顯示錯誤訊息且 App 回到可操作狀態，**不會停在全黑畫面**。
12. **Android 圖示**：於 Android 裝置加入主畫面，確認圓形遮罩下頂部日蝕完整、沒有被削掉。
13. **連點按鈕**：快速連點同一顆按鈕數次，確認該按鈕的下拉選單／提示不會被裁切。
