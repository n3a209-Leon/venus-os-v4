# LIMU v20.63 — Venus Refinement 效能與互動版

本版集中處理 iPhone 首次起滑、長頁重繪、面板開關與觸控回饋；不變更
Venus OS、Firebase、資料格式、總覽座號資訊、五套主題版面或安全區。

## 1. 班級設定首次起滑

- 從「更多」開啟班級設定時，先完成面板關閉與 `body` 捲動解鎖，再掛上設定長頁。
- 不再讓舊頁捲動位置、scroll anchoring 與新設定頁在同一次畫面提交中互相拉扯。
- 班級／組別／幹部分頁明確使用 `touch-action: pan-y`，讓垂直拖曳優先交給原生捲動。
- 已選取的分頁保持不參與命中測試；從「班級」按鈕中央起滑也不會攔截手勢。
- 手機版仍只保留整頁一個捲動層，不恢復巢狀捲動容器。

## 2. 設定長頁重繪隔離

- `SettingsView` 改由 `React.memo` 保留，只有班級、組別、幹部、學期或行事曆資料實際改變才重繪。
- 父層的同步狀態、其他面板開關與非設定資料更新，不再重建整個班級設定 DOM。
- 保留原有輸入內容、焦點、分頁與展開狀態，不改任何儲存流程。

## 3. 導覽與面板效能

- `lastView` 由同步儲存改為手勢結束後的閒置寫入；快速連續切換時只保留最後一次狀態。
- 面板底色同步第一次成功後立即停止，不再固定於 150ms／500ms 重複掃描面板與讀取 layout。
- 功能面板進入「歷年檔案」「班級設定」及回首頁，統一採先解鎖、後換頁的雙階段導覽。

## 4. 觸控回饋

- 移除作業選擇器與週計畫新增鈕中多組 `touchstart`／`mousedown` 行內濾鏡監聽。
- 改成純 CSS 的縮放與透明度回饋，不在每次按下時寫入 `style.filter` 或建立昂貴光暈。
- 操作位置、按鈕尺寸、功能與選取結果完全不變。

## 5. Venus OS Toast

- 一般完成訊息改為 44px 高、非阻塞、完全不攔截觸控的 Venus Toast。
- Toast 會跟隨預設、NERV、慕夏、星雲與奧德賽主題色調。
- 同時最多顯示三則，使用者可繼續捲動或操作，不必再按「好」。
- 刪除、覆蓋、未加密匯出、資料匯入與其他需要決策的流程仍保留原生確認視窗。
- 錯誤與資料安全警告仍使用明確的阻塞式提示，不會被短暫 Toast 取代。

## 保留不動

- Venus OS 與 Venus Card
- Firebase 專案、Firestore 路徑、UID 分區與同步衝突邏輯
- IndexedDB／localStorage 資料內容與 `schemaVersion 7`
- 總覽上的個別座號資訊，以及 5／6／7 組動態欄位
- iPhone 頂部安全區、底部懸浮膠囊位置與內容底部預留
- v20.61 的日期／行事曆／核心快取，以及 v20.62 的主題美術與 SVG 圖示

## 驗證

- 10 段內嵌程式碼、Service Worker 與兩支工具程式語法通過。
- CSS 由正式解析器完整解析，Toast 與互動規則沒有結構錯誤。
- 430×932、3× DPR 的真實 Chromium 回歸測試 42／42 通過。
- 功能選單進入班級設定測得約 219ms；設定頁已解除 modal 捲動鎖並可立即捲動。
- 已選「班級」分頁為 `pointer-events:none`、`touch-action:pan-y`，起滑不被按鈕攔截。
- Venus Toast 為 44px、`pointer-events:none`，三套重製主題仍無橫向溢出。
- Chromium 不能完整模擬 Safari 的慣性與安全區；部署後仍建議在 iPhone 從
  「更多 → 班級設定 → 班級」按鈕中央直接上滑一次，作為最終真機確認。
