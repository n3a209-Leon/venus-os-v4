# LIMU v20.51

## 根本原因（已確認，不再是推測）

iOS 每次啟動給網頁的畫布高度，會在 **873pt 與 932pt 之間隨機跳**。

| 版本 | 該次啟動的量測 | 有無黑塊 |
|---|---|---|
| v20.49 | `inn932 cli873 vis932 dvh932 ovl932` | 有（因為當時圖層被 transform 提升成合成層） |
| v20.50 | `inn873 cli873 vis873 dvh873 ovl873 wrp873 img873` | 有（畫布真的只有 873） |
| v20.46 | `inn932 … ovl932 wrp932` | 有 |

`lvh` 恆為 932，其餘全部會跳。抽到 873 的那次，螢幕仍是 932，
**多出來的 59pt 不屬於網頁**，由 iOS 用頁面底色自行填滿——任何 CSS 都
畫不到那裡。這一點 v20.48 就已經確認過。

### 這題花了十版的原因

1. **症狀是隨機的，而每次觀察都被當成固定事實。**
   抽到 932 的啟動看起來像「修好了」，抽到 873 的看起來像「沒修好」，
   於是每一版的結論都被下一次啟動推翻。
2. **v20.48 已經得到正確結論，卻在 v20.49 自行推翻。**
   當時看到獎卡系統的截圖畫滿 932，就認定「同一台裝置可以畫滿，
   所以一定是 LIMU 的 CSS 有問題」——但那張截圖很可能只是剛好抽到 932。
   我拿一次觀察去否定一個已經成立的結論。
3. **從頭到尾沒有先確認症狀的重現率。**
   第一輪若請使用者連開五次截圖，立刻就會看到它時有時無，
   後面九個版本的方向完全不會走偏。

## 修正

既然那 59pt 畫不到，就讓它**看不出來**。

實測數據：條帶顏色 `rgb(26,26,28)`，圖片底緣 `rgb(29,28,31)`——只差 3 階。
所以視覺上察覺到的並不是色差，而是**水波紋理突然變成一片平面**。

因此讓影像在底部自然收成同一個平面色：

```css
linear-gradient(180deg,
  rgba(2,5,9,.18) 0%, transparent 22%, transparent 62%,
  rgba(26,26,28,.22) 78%, rgba(26,26,28,.66) 92%, rgb(26,26,28) 100%)
```

- 抽到 873 → 影像底部已經收成 `rgb(26,26,28)`，與下方那條無縫銜接
- 抽到 932 → 只是一道收邊暗角

`html.limu-splash-bg` 的底色同步改為 `rgb(26,26,28)`，兩者完全一致。

v20.31 曾因為這裡收得太重（`.82` 近乎純黑）而拿掉；這次收的是與底色一致的
`rgb(26,26,28)`，不是純黑，且到 78% 才開始，標題區同時獲得更好的可讀性。

## 一個可以否證我的測試

如果我的解釋正確，**獎卡系統在抽到 873 的那次啟動，也會出現同樣的條帶。**
請把獎卡系統完全關閉、重開五次，每次截圖底部。
若五次都完好，代表我的解釋仍有缺口，屆時改以獎卡系統的靜態 HTML 開場結構
重寫 LIMU 的開場（兩者最後一個結構差異）。

---

## 驗證

- 開場 class 生命週期正確
- 正常環境端對端 32/32
- 無 IndexedDB 降級 7/7
- CSS 解析零錯誤
