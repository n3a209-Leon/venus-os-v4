# LIMU v20.68 — 「更多」面板一致化

版本：20.68.0
建置：limu-teacher-v20-68-20260804
快取：hw-tracker-v20-68
資料格式：7（未變動）

累積包含 v20.65～v20.67。

---

## 起因

實機截圖比對發現兩件事：

1. 從月曆開「更多」，少了「技術診斷」「獎卡資料掃描」——這兩項寫了
   `hidden: view !== 'day'`，只在當日紀錄畫面出現。
2. **從當日紀錄開「更多」，其實也少了一項**：最底下的「開啟獎卡系統」。
   它不是沒渲染，是被擠出可視範圍。底部回首頁圓鈕是 `position:sticky; bottom:0`
   且底色不透明，把清單切得乾乾淨淨，看起來就像到「匯入行事曆」為止。

## 量測

iPhone 15 Pro（393×852pt），面板上限 `88dvh` = 750pt：

| 區塊 | 高度 |
|---|---|
| 面板上緣留白 + 標題 | 52 |
| 帳號列 | 59 |
| 回首頁區（sticky，永遠佔位） | 80 |
| 底部留白 + 安全區 | 48 |
| **固定佔用** | **239** |

剩給清單 **511pt**，每列 61pt：

- 8 項 = 488pt → 全部看得到（月曆版截圖相符）
- 10 項 = 610pt → 超出約 1.6 列（當日紀錄版截圖相符）

**十項要同時看到需要 849pt，比 852pt 的整支螢幕還高**——就算面板拉到
`100dvh` 也塞不進去。這不是調參數能解決的，所以維持可捲動，改為把
「下面還有」做得看得見。

---

## 三項變更

### 1. 十個項目固定顯示

移除「技術診斷」「獎卡資料掃描」的 `hidden` 條件。不論從哪個畫面開啟，
「更多」的內容都一樣，位置不會跳動。

### 2. 從任何畫面都能執行當日工具

這兩項是用 `document.querySelector('[data-day-tool="…"]')` 去點當日紀錄
畫面裡的按鈕。既然改成常駐，「不在當日紀錄」就從錯誤情境變成正常情境：

- 已在當日紀錄 → 直接點（面板是覆蓋層，底下按鈕還在）
- 不在 → 先開今天的當日紀錄，畫面掛好後自動執行

**為什麼用 `ref` + `useEffect` 而不是 `setTimeout` 重試**：`openDay` 是非同步的，
重試要猜幾毫秒才夠——猜短了沒接到、猜長了使用者乾等。`useEffect` 依賴 `view`，
React 完成 DOM commit 之後才會跑，按鈕保證已經在畫面上。這是確定的時點，
不是估計值。

`openDay` 失敗（含 promise rejection）會清掉待辦並跳提示，不會靜默卡住。

沿用 v20.63 的雙階段導覽：先收面板、解除 body 捲動鎖，隔一個畫面週期再掛新頁。

### 3. 回首頁區上緣改為漸層淡出

`.gear-home-dock::before` 的漸層由面板底色本身構成，被蓋住的那一項會漸漸消失
而不是被切斷——這是使用者唯一看得懂的「可以再往下滑」訊號。

底色抽成單一變數，面板與漸層共用同一個來源，五套主題自動一致。

漸層起點用「同色 alpha 0」而不是 `transparent`：後者在部分繪圖引擎會經由
`rgba(0,0,0,0)` 內插，中間帶出一條灰邊。兩端只差透明度就不會有這個問題。

---

## 驗證紀錄

- 11 段內嵌程式 + 合併後全域範圍語法：通過
- ESLint 錯誤規則：30 項，與 v20.64 起完全相同，四個版本下來未新增任何一項
- `react-hooks/rules-of-hooks`：零違反（新增的 `useEffect` 位於 App 頂層，非條件式）
- 逐項斷言 17 項全過（含 v20.65～v20.67 全部修正仍在位）
- 版本識別一致性、預快取 28 項、HTML 標籤配對：通過

**未做真實瀏覽器驗證**——Chromium 在本次作業環境無法下載。

### 建議的實測順序

1. 從**月曆**開「更多」→ 應該看到十項，最後一項「開啟獎卡系統」需要往下滑一點；
   滑之前上方應該看得出漸層淡出。
2. 從月曆點「技術診斷」→ 應該自動跳到今天的當日紀錄並直接開啟診斷。
3. 從**當日紀錄**開「更多」→ 內容與步驟 1 完全相同。
4. 五套主題各開一次，確認漸層顏色與面板底色一致、沒有色帶。
5. `?perf=1` 的月曆「格子重建」讀數——v20.65 至今仍是唯一未驗收項目。
