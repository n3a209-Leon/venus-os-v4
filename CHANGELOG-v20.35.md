# LIMU v20.35（iPhone 原生捲動優先修正版）

本版以 v20.34 正式精簡版為基礎，處理 iPhone 滑動時「停住、恢復、再停住，像被拉住」的殘留問題；不新增功能，不變更資料格式。

## 滑動修正

- 移除手指接觸／捲動時對整頁切換 `limu-user-scrolling` class 的做法，避免 Safari 每次手勢都重新計算所有元素與偽元素的動畫、濾鏡與合成圖層。
- 移除 PWA 啟動後動態注入的全域 `* { touch-action: manipulation !important; }`，保留既有按鈕點擊能力，讓原生頁面捲動取得穩定優先權。
- 將日期左右箭頭改為允許垂直原生捲動；手指開始拖曳時立即取消長按連續換日，避免捲動中觸發日期重繪。
- 手機版在所有長頁區塊停用 `content-visibility:auto`，包含月摘要、出勤、抵免、回條、封存與設定內容；避免 Safari 滑到新區塊時才臨時建立版面。
- 滑動優先排程器只記錄手勢／捲動時間，不再於每個 scroll 事件改 DOM、class 或樣式。
- 移除背景工作「超時後即使仍在滑動也強制執行」的路徑；appState、dirty-map、localStorage 備援與同步會等手勢及慣性捲動真正結束後才執行。
- 頁面離開或切到背景時仍會立即 flush，資料安全機制保留。

## 保持不變

- 不含 `?perf`、⏱ 按鈕、效能探針或正式部署 QA 程式。
- 資料 schema 維持 7。
- 總覽座號、現有按鍵、班級 ID、Firebase 文件路徑、主題內容與 Venus OS 均未更動。
