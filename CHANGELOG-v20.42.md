# LIMU v20.42

## 從功能選單進入班級設定時，頁面會被拉住／黏住

### 症狀

「更多 → 班級設定」進到班級頁後，第一次滑動有被拉住、黏住、滑不太動的感覺，
過一下就恢復正常。

### 原因

捲動鎖（`shouldLockPage`）的還原對象搞錯了頁面。

流程是這樣的：

1. 點「更多」→ `showGearMenu = true` → 捲動鎖啟動：
   `body{position:fixed; top:-800px}`，同時記下「開選單當下」的捲動位置 800px。
2. 點「班級設定」→ `setView('settings')` 與 `setShowGearMenu(false)`
   在同一批狀態更新送出。
3. React 先掛上新的班級設定頁，**之後**才跑捲動鎖的 cleanup。
4. cleanup 執行 `window.scrollTo(0, 800)`——把上一頁的偏移，
   套用到了剛進來的班級設定頁。

於是新頁一進來就停在中段。而此時版面還在長（班級清單、幹部名單都是
非同步載入），瀏覽器的捲動錨定會持續修正位置，手指一滑就被拉回去，
摸起來就是被黏住；等版面長完就恢復正常——所以「只有一開始」。

### 修正

新增一個以 `view` 為依賴的 effect，切換主檢視時把捲動歸零：

```js
useEffect(function() {
    try { window.scrollTo(0, 0); } catch(e) {}
}, [view]);
```

宣告位置在捲動鎖之後。React 在同一次 commit 中會先跑完所有 effect 的
cleanup，才輪到 effect 本體，所以這裡的歸零一定晚於上面的錯誤還原，
能確實蓋掉它。

換頁後回到頂端本來就是這類 App 該有的行為，順帶修掉「從設定返回月曆時
停在奇怪位置」的同源問題。

---

## 驗證

- 正常環境端對端 32/32
- 無 IndexedDB 降級 7/7
- 種入班級後 145 個按鈕與月曆格自動巡覽，零錯誤、元件樹未卸載
- 各 script 區塊語法檢查、CSS 解析皆零錯誤
- sw.js 預快取 23 個項目對照實體檔案，全數存在

實機仍需確認：進入班級設定時是否還會被拉住。

## 附記

打包時發現 `sw.js` 曾被一行有副作用的批次改版指令清成 0 byte
（`open(w)` 先截斷檔案、`open(r).read()` 才執行），已從 v20.41 封存包還原
並重新套用版本號，內容與預快取清單都已逐項核對。這一版的 `sw.js` 是完整的。
