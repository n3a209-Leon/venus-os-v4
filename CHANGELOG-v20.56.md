# LIMU v20.56

## 1）回首頁鈕移到左下角

原本放在標題列右側、緊鄰關閉鈕——單手操作時拇指要跨越整個螢幕，
而且和 `×` 擠在一起容易誤按。

改為固定在**畫面左下角**（`position:fixed`，左緣與下緣各留 18px 並吃安全區），
左手拇指的自然落點。`z-index:9500` 高於面板本身，捲動時不會跟著跑掉。

按鈕加大到 `min-height:46px`、`padding:11px 20px`，外框用 `currentColor`，
底色 `rgba(125,125,150,.22)`——深淺兩種主題的面板上都看得清楚。

三個面板（今日工作台、輔導紀錄中心、全域搜尋）共用 `.sheet-home-btn`。

## 2）工作欄改為純圖示

依你的要求拿掉文字，圖示放大到 22px，選取態保留填色圓形＋圖示放大 8%。

文字沒有刪除，而是改用視覺隱藏（`clip-path:inset(50%)` 的標準做法）
繼續留給螢幕報讀器，`aria-label` 也還在。畫面乾淨，但輔助技術仍讀得到
「開啟月曆紀錄」這類完整描述。

## 3）跳窗下半部的黑面

兩個原因疊在一起：

1. v20.55 為了讓面板內容避開懸浮工作欄，多留了 58px 底部空白。
2. 面板本來就蓋住整個下半部，工作欄留在後面既看不到也沒用。

修正：**面板開啟時直接收起工作欄**（`body.v18-modal-open .mobile-dock{display:none}`），
底部留白改回單純吃安全區。那塊多出來的黑面消失，面板也不再需要避讓。

---

## 驗證

- 跳窗開啟時 `body.v18-modal-open` 正確加上、關閉後移除
- 左下角回首頁鈕存在、可按、點擊後關窗並回月曆
- 工作欄 5 個按鈕、標籤視覺隱藏但保留於 DOM
- 正常環境端對端 32/32
- 無 IndexedDB 降級 7/7
- 116 個按鈕與月曆格巡覽零錯誤
- CSS 解析零錯誤
