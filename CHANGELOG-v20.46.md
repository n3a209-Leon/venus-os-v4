# LIMU v20.46

## 定案：不是圖片大小，是視窗高度在啟動瞬間不穩定

兩次實機量測擺在一起就清楚了：

| | 第一次（v20.44） | 第二次（v20.45） |
|---|---|---|
| `scr` 螢幕 | 932 | 932 |
| `inn` innerHeight | **932** | **873** |
| `cli` clientHeight | 873 | 873 |
| `vis` visualViewport | **932** | **873** |
| `dvh` | **932** | **873** |
| `svh` | 873 | 873 |
| **`lvh`** | **932** | **932** |
| `ovl` 開場層 | 932 | 873 |
| `wrp` 圖層 | — | 873 |

同一台裝置、同一個 App，兩次啟動量到的視窗高度不一樣。
`innerHeight`、`visualViewport`、`dvh` 全都在 932 與 873 之間跳；
開場層忠實照著當下的值算，所以第二次就只有 873，
底部空出 59pt 露出自己的背景色。

**圖片本身沒有問題。** `img945` 是 873 × 1.082（開場動畫的
`scale(1.085)`，`getBoundingClientRect` 會把 transform 算進去），
換算回排版高度正好是 873——圖片一直忠實填滿被告知的高度，
是「被告知的高度」錯了。

原因是 iOS 獨立 App 在啟動的極早期，安全區與視窗尺寸尚未定案，
此時讀到的是不含頂部安全區的短視窗（873 = 932 − 59，正好等於 `svh`）。
開場畫面掛載得太早，剛好踩在這個空窗期，而且踩不踩得到看當次啟動的時序，
所以時好時壞。

### 修正

**表格裡只有 `lvh` 兩次都是 932。** `lvh` 是「工具列全部收起時的最大視窗
高度」，在獨立 App 模式下就等於整個螢幕，是靜態值，不受啟動當下的
過渡狀態影響。

開場層、圖層、圖片三處統一改用：

```css
height:100vh;    /* 舊瀏覽器 */
height:100lvh;   /* 兩次量測都穩定回報 932 */
```

### 追加量測

量測列多印一行 `late`：1.2 秒後重新量 `innerHeight` / `clientHeight` /
圖層高度。若 `late` 的數字與上面那行不同，就直接證實了「啟動瞬間視窗
尺寸尚未定案」這個結論；若相同，代表這次啟動沒踩到空窗期。
兩種結果都有用。

---

## 驗證

- 正常環境端對端 32/32
- 無 IndexedDB 降級 7/7
- JS 各區塊語法、CSS 解析零錯誤

## 部署後

看開場左上角：黑塊應消失，`ovl` 與 `wrp` 應為 932。
確認後下一版移除整段量測列。
